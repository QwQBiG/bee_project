# Bee Vision 其余 Python 依赖清单
# torch 与 torchvision 需要先按 packages/README.md 单独安装，以确保使用 CUDA 13.2 版本。
# ByteTrack 适配层按此版本验证；不要用 >=，以免上游 API 变化。
ultralytics==8.4.96
lap==0.5.13

# 视频、数值计算与报告
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
pyyaml>=6.0
matplotlib>=3.7.0

# 项目测试
pytest>=7.0.0
