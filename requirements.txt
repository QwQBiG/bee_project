# Bee Vision 唯一的 Python 依赖清单
# start_web.bat 会先根据显卡安装正确的 PyTorch CPU/CUDA 构建，
# 然后再安装本文件中的其余依赖。

# 深度学习与目标检测
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# 视频、数值计算与报告
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
pyyaml>=6.0
matplotlib>=3.7.0

# Web 服务
fastapi>=0.116.0
uvicorn>=0.35.0
python-multipart>=0.0.20

# 项目测试与本地接口测试
pytest>=7.0.0
httpx>=0.27.0
