services:
  bee-vision:
    build: .
    ports:
      - "8000:8000"
    environment:
      BEE_DEVICE: cpu
      BEE_MAX_UPLOAD_MB: 4096
    volumes:
      - bee-results:/app/runtime_results
    restart: unless-stopped

volumes:
  bee-results:
